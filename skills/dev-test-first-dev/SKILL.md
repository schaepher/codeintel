---
name: dev-test-first-dev
license: 'MIT'
description: '测试先行开发工作流：先写测试（红）→ 实现（绿）→ 全量回归（-race）→ 验证后 git push；断言要能数值区分新旧行为；负例优先 + 形态矩阵（变体×传递×写入）枚举；最小复现 bug 报告（行号+根因+预期）；复杂需求先设计树访谈（编号问题+推荐答案+一行拍板）。当开始新功能、修 bug 或收到模糊需求时使用本 skill。'
---

# 测试先行开发工作流

测试先行让行为契约先于实现固定，避免实现后补测试的盲区。

## 〇、开发纪律（前置，每次开发都适用）

1. **任何疑问先确认，不擅自决定**（用户明确要求）：开发中遇到设计文档
   未覆盖或与其冲突的点（阈值、数据来源、ID 格式、范围增减），先向用户
   说明并给出选项让其确认，确认后再实施；**发现设计文档内部矛盾要报告，
   不静默消化**
2. **所有命令设置超时**（用户明确要求）：`timeout N <命令>`——不能无限
   等待；挂起时以 timeout 终止并定位挂起点
3. **验证用自建小示例**：/tmp 下最小 fixture（秒级）迭代，不用大仓库全量
   验证（每次数分钟成本太高）；大仓库仅用于最终验收

## 一、标准流程（红→绿→全量回归→push）

```
1. 写测试：先写会失败的测试（红）——断言要具体、能区分新旧行为
2. 实现：只写让测试通过的最小代码（绿）
3. 全量回归：go test -race -count=1 ./...（或项目等价命令）
4. 验证后 git push（"改完并验证完后要 git push"）
```

配套约定：设置超时/后台执行、`make test`（或等价 Makefile 目标）作为
统一入口（`go test -race -count=1 -cover ./...`）。

## 二、断言要能数值区分新旧行为

行为契约变更时，第一步改写/新增**能区分新旧行为的精确断言**：

- 反例（模糊）："节点变多了"
- 正例（精确）："分组新增恰好 7 个节点"——多一个即说明行为越界，
  恰好 N 个才符合契约，契约无法被悄悄破坏

## 三、负例优先 + 形态矩阵

识别/判定类需求（如"识别某类调用"）：

1. 先写"**不应当识别**"的负例（如误报后补：某种形态必须排除）
2. 按**形态矩阵**枚举变体：起点×传递方式×写入形态（如同函数/跨函数/
   嵌套/闭包/对象级），每格至少一个用例
3. **维度按指令全集系统枚举，不能凭经验列格子**——凭经验列格子会漏
   变体，被用户提醒才发现
4. **功能分两层验证**：展示层（结构/边/节点本身）+ **数据流贯通层**
   （追踪经过该结构是否连通）——只测展示层会漏数据流不贯通的问题
5. **"设计注释"不能当事实**：代码里"无法解析/跳过/不支持"这类注释
   只是当时的实现决定——每个这类注释都要问一句"其实能不能？"
   （如接口调用其实可枚举模块内候选实现）
6. 实现后主动输出"当前覆盖 X/Y，未覆盖 W"请用户确认

**教训**：反复往返（同一问题多轮追问）几乎都源于"形态枚举不全"——
一次只做矩阵的一格。

**复盘要求**：用户报告问题时，先复盘"为什么验证没覆盖该形态"，补用例
再修功能（见 dev-verify-and-accept 的主动自检）。

## 四、最小复现 bug 报告（用户侧给 AI 的输入规范）

报 bug 时自带三要素，让 AI 直接实施而非排查：

1. **完整最小复现**（文件树/源码）
2. **精确根因定位**（文件:行号 + 原因 + P0/P1 优先级）
3. **预期行为**

示例："根因在 internal/.../summary_iface.go:91 … 只识别直接的
*ssa.Const，请修复为支持间接形态。"

效果：完整复现 + 精确根因 → 一次实施通过、零返工。

## 五、复杂需求：先设计树访谈再实现

收到模糊/多解需求时，用**编号问题 + 推荐答案**流程收敛：

1. AI 一次性穷举设计决策点，每个编号，附推荐答案
2. 用户一行简短作答快速拍板（如 "a c a a\n支持从路由表读取"）
3. 决策落档后实施

效果：多轮收敛、设计文档一次通过、实施零需求返工。

配合〇节的"任何疑问先确认"——设计树访谈是其规模化形态（穷举而非
逐个追问）。

## 已知坑

- **别把"单测全绿"当"真实可用"**——单测覆盖不了的真实问题见
  dev-verify-and-accept；测试先行与真实验收是两回事
- fixture 行号是 AI 的机械弱点（多轮错位）——用解析器动态取
  位置或"存在性"断言，别手数行号
- 接口形态链问题（mock 接口"假绿"掩盖真实形态）——单测的 mock 要与
  真实形态对齐，见 dev-verify-and-accept 的双向核对
